# Issue 2: Project Foundation, Next.js Setup & Design System

**What to build:** Next.js App Router setup, Tailwind CSS configuration with custom design system tokens mirroring the physical ledger aesthetic (`docs/sample ui.html`), typography (Inter and JetBrains Mono), and root layout.

**Blocked by:** None (can start immediately)

**Status:** completed

- [x] Next.js App Router and project configuration (`next.config.ts`, `tsconfig.json`, `package.json`).
- [x] Tailwind CSS and custom styling design tokens (`app/globals.css`).
- [x] Root layout with Geist fonts and responsive container (`app/layout.tsx`).
